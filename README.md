# 🧠 CBT Therapy App

PWA-приложение для когнитивно-поведенческой терапии (КПТ), фокусирующееся на поддержке восстановления от зависимостей.

## ✨ Функционал

### Основные модули
- 📝 **Дневник эмоций** — записи настроения с привязкой к триггерам
- 🧠 **КПТ упражнения** — когнитивная реструктуризация
- 🛡️ **Трекер трезвости** — счётчик дней с детализацией срывов
- ⚡ **Триггеры** — управление и отслеживание триггеров

### КПТ техники
- 🌬️ **Дыхательные упражнения** — 4-7-8, Box Breathing
- 🌍 **Заземление 5-4-3-2-1** — техника при тревоге
- 🃏 **Копинг-карточки** — быстрые ответы на негативные мысли

### Аналитика и геймификация
- 📊 **Dashboard** — графики настроения, корреляции триггеров
- 🏆 **Бейджи** — 13 достижений за прогресс

## 🏗️ Структура проекта

```
cbt2/
├── backend/          # Node.js + Express API, JWT auth, PostgreSQL
├── frontend/         # React 18 + TypeScript, Tailwind CSS, PWA
├── docker-compose.yml
└── nginx/           # Production reverse proxy
```

## 🚀 Быстрый старт

### 1. Запуск с Docker
```bash
docker compose up -d --build
```

### 2. Локальная разработка
```bash
# Backend
cd backend
npm install
npm start

# Frontend
cd frontend
npm install
npm start
```

**Backend:** `http://localhost:4000`  
**Frontend:** `http://localhost:3000`

## 🔧 Конфигурация

Скопируйте `.env.example` в `.env` и настройте переменные:

```env
DATABASE_URL=postgresql://user:pass@localhost:5432/cbt_db
JWT_SECRET=your-secret-key
```

## 📱 PWA

Приложение поддерживает установку на устройства и работу офлайн.

## 📄 Лицензия

MIT

